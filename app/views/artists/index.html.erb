<h1> All Artists </h1>

    <ul>
        <% @artists.each do |artist| %>
            <li><%= link_to "#{artist.name} - #{artist.song_count} songs", artist_path(artist) %></li>
        <% end %>
    </ul>
